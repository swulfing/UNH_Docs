mean(as.numeric(ceph_sites2$AVG_WT), na.rm = FALSE)
mean(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE)
win.data <- list(mass   = ceph_sites2$AVG_WT,
site   = ceph_sites2$id,
n      = nrow(ceph_sites2),
nsites = length(unique(ceph_sites2$id)),
stdev_data = sd(ceph_sites2$AVG_WT),
mean_data = mean(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE)))
win.data <- list(mass   = ceph_sites2$AVG_WT,
site   = ceph_sites2$id,
n      = nrow(ceph_sites2),
nsites = length(unique(ceph_sites2$id)),
stdev_data = sd(ceph_sites2$AVG_WT),
mean_data = mean(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE))
sd(ceph_sites2$AVG_WT)
sd(as.numeric(ceph_sites2$AVG_WT))
sd(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE)
class(ceph_sites2$AVG_WT)
win.data <- list(mass   = as.numeric(ceph_sites2$AVG_WT),
site   = ceph_sites2$id,
n      = nrow(ceph_sites2),
nsites = length(unique(ceph_sites2$id)),
stdev_data = sd(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE),
mean_data = mean(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE))
# Initial values
inits <- function()list(alpha = rnorm(7), sigma = rlnorm(1))
# Parameters monitored
params <- c("alpha",
"sigma"
#"Bayes.P"
#"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
sink("cephs_sitewt.txt")
cat("
model {
# PRIORS
for(i in 1:nsites){       #loop for alphas. can also do alpha[1] alpha[2]
alpha[i] ~ dnorm(mu_site,tau_site)
}
mu_site ~ dnorm(0,0.001)
tau_site <- 1/sigma_array^2
sigma_site ~ dunif(0,100)
for(i in 1:nsites){
tau[i] <- 1/sigma[i]^2
sigma[i] ~ dunif(0,100)
}
# LIKELIHOOD
for(i in 1:n){
mass[i] ~ dnorm(mu[i],tau[site[i]])
mu[i] <- alpha[site[i]] #Mean will vary by site. 3 alphas, 1 per site. loop through every row of your dataset.
} # i
# DERIVED QUANTITIES
for(i in 1:nsites){
avg[i] <- (alpha[i]*stdev_data) + mean_data
stan_dev[i] <- (sigma[i]*stdev_data)
}
mu_array_m <- (mu_site*stdev_data) + mean_data
sigma_array_m <- (sigma_site*stdev_data)
} # end of model
",fill = TRUE)
sink()
win.data <- list(mass   = as.numeric(ceph_sites2$AVG_WT),
site   = ceph_sites2$id,
n      = nrow(ceph_sites2),
nsites = length(unique(ceph_sites2$id)),
stdev_data = sd(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE),
mean_data = mean(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE))
# Initial values
inits <- function()list(alpha = rnorm(7), sigma = rlnorm(1))
# Parameters monitored
params <- c("alpha",
"sigma"
#"Bayes.P"
#"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
out_stwt <- jags(win.data, inits, params, "cephs_sitewt.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
sink("cephs_sitewt.txt")
cat("
model {
# PRIORS
for(i in 1:nsites){       #loop for alphas. can also do alpha[1] alpha[2]
alpha[i] ~ dnorm(mu_site,tau_site)
}
mu_site ~ dnorm(0,0.001)
tau_site <- 1/sigma_site^2
sigma_site ~ dunif(0,100)
for(i in 1:nsites){
tau[i] <- 1/sigma[i]^2
sigma[i] ~ dunif(0,100)
}
# LIKELIHOOD
for(i in 1:n){
mass[i] ~ dnorm(mu[i],tau[site[i]])
mu[i] <- alpha[site[i]] #Mean will vary by site. 3 alphas, 1 per site. loop through every row of your dataset.
} # i
# DERIVED QUANTITIES
for(i in 1:nsites){
avg[i] <- (alpha[i]*stdev_data) + mean_data
stan_dev[i] <- (sigma[i]*stdev_data)
}
mu_array_m <- (mu_site*stdev_data) + mean_data
sigma_array_m <- (sigma_site*stdev_data)
} # end of model
",fill = TRUE)
win.data <- list(mass   = as.numeric(ceph_sites2$AVG_WT),
site   = ceph_sites2$id,
n      = nrow(ceph_sites2),
nsites = length(unique(ceph_sites2$id)),
stdev_data = sd(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE),
mean_data = mean(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE))
# Initial values
inits <- function()list(alpha = rnorm(7), sigma = rlnorm(1))
# Parameters monitored
params <- c("alpha",
"sigma"
#"Bayes.P"
#"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
out_stwt <- jags(win.data, inits, params, "cephs_sitewt.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
# Initial values
inits <- function()list(alpha = rnorm(7), sigma = rep(rlnorm(1),7))
# Parameters monitored
params <- c("alpha",
"sigma"
#"Bayes.P"
#"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
out_stwt <- jags(win.data, inits, params, "cephs_sitewt.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
sink("cephs_sitewt.txt")
cat("
model {
# PRIORS
for(i in 1:nsites){       #loop for alphas. can also do alpha[1] alpha[2]
alpha[i] ~ dnorm(mu_site,tau_site)
}
mu_site ~ dnorm(0,0.001)
tau_site <- 1/sigma_site^2
sigma_site ~ dunif(0,100)
for(i in 1:nsites){
tau[i] <- 1/sigma[i]^2
sigma[i] ~ dunif(0,100)
}
# LIKELIHOOD
for(i in 1:n){
mass[i] ~ dnorm(mu[i],tau[site[i]])
mu[i] <- alpha[site[i]] #Mean will vary by site. 3 alphas, 1 per site. loop through every row of your dataset.
} # i
# DERIVED QUANTITIES
for(i in 1:nsites){
avg[i] <- (alpha[i]*stdev_data) + mean_data
stan_dev[i] <- (sigma[i]*stdev_data)
}
mu_array_m <- (mu_site*stdev_data) + mean_data
sigma_array_m <- (sigma_site*stdev_data)
} # end of model
",fill = TRUE)
# Initial values
inits <- function()list(alpha = rnorm(7), sigma = rep(rlnorm(1),7))
# Parameters monitored
params <- c("alpha",
"sigma"
#"Bayes.P"
#"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
out_stwt <- jags(win.data, inits, params, "cephs_sitewt.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_stwt, dig = 1)
knitr::opts_chunk$set(echo = TRUE)
library(R2jags)
library(tidyr)
library(dplyr)
setwd("~/UNH_Docs/NR913")
cephs <- read.csv("C:/Users/sophi/Documents/UNH_Docs/Proposal/CNH2_data/cleanData.csv")
site_data <- cephs %>%
group_by(REGION) %>%
summarise(Count = n()) %>%
filter(Count > 150)
ceph_sites <- cephs %>%
filter(REGION %in% site_data$REGION)
ceph_sites2 <- transform(ceph_sites,id=as.numeric(factor(REGION)))
ceph_model <- transform(cephs,id=as.numeric(factor(REGION)), sex_id=(as.numeric(factor(FISHER_SEX))-1))
ceph_sex <- ceph_model %>% drop_na(sex_id)
#Questions about this one: can you do a p val for an anova? Does the calc look the same?
#wANT TO REMAKE data next
#Add random effects? can you do that to an anova
#ANOVA
#Only looking at most fished sites
#Different variances?
#Visualize Data-FIX THIS
boxplot(as.numeric(AVG_WT) ~ REGION,
ylab = "Average wt of Cepalopod",
xlab = "Site",
# outline = TRUE,
# notch = FALSE,
# staplewex = 0.2,
# boxwex = 0.4,
# border = TRUE,
data = ceph_sites2)
sink("cephs_sitewt.txt")
cat("
model {
# PRIORS
for(i in 1:nsites){       #loop for alphas. can also do alpha[1] alpha[2]
alpha[i] ~ dnorm(mu_site,tau_site)
}
mu_site ~ dnorm(0,0.001)
tau_site <- 1/sigma_site^2
sigma_site ~ dunif(0,100)
for(i in 1:nsites){
tau[i] <- 1/sigma[i]^2
sigma[i] ~ dunif(0,100)
}
# LIKELIHOOD
for(i in 1:n){
mass[i] ~ dnorm(mu[i],tau[site[i]])
mu[i] <- alpha[site[i]] #Mean will vary by site. 3 alphas, 1 per site. loop through every row of your dataset.
} # i
# DERIVED QUANTITIES
for(i in 1:nsites){
avg[i] <- (alpha[i]*stdev_data) + mean_data
stan_dev[i] <- (sigma[i]*stdev_data)
}
mu_site_m <- (mu_site*stdev_data) + mean_data
sigma_site_m <- (sigma_site*stdev_data)
} # end of model
",fill = TRUE)
sink()
win.data <- list(mass   = as.numeric(ceph_sites2$AVG_WT),
site   = ceph_sites2$id,
n      = nrow(ceph_sites2),
nsites = length(unique(ceph_sites2$id)),
stdev_data = sd(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE),
mean_data = mean(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE))
# Initial values
inits <- function()list(alpha = rnorm(7), sigma = rep(rlnorm(1),7))
# Parameters monitored
params <- c("alpha",
"sigma",
"avg",
"stan_dev",
"mu_site_m",
"sigma_site_m"
#"Bayes.P"
#"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
out_stwt <- jags(win.data, inits, params, "cephs_sitewt.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_stwt, dig = 1)
knitr::opts_chunk$set(echo = TRUE)
library(R2jags)
library(tidyr)
library(dplyr)
setwd("~/UNH_Docs/NR913")
cephs <- read.csv("C:/Users/sophi/Documents/UNH_Docs/Proposal/CNH2_data/cleanData.csv")
site_data <- cephs %>%
group_by(REGION) %>%
summarise(Count = n()) %>%
filter(Count > 150)
ceph_sites <- cephs %>%
filter(REGION %in% site_data$REGION)
ceph_sites2 <- transform(ceph_sites,id=as.numeric(factor(REGION)))
ceph_model <- transform(cephs,id=as.numeric(factor(REGION)), sex_id=(as.numeric(factor(FISHER_SEX))-1))
ceph_sex <- ceph_model %>% drop_na(sex_id)
sink("cephs_sitewt.txt")
cat("
model {
# PRIORS
for(i in 1:nsites){       #loop for alphas. can also do alpha[1] alpha[2]
alpha[i] ~ dnorm(mu_site,tau_site)
}
mu_site ~ dnorm(0,0.001)
tau_site <- 1/sigma_site^2
sigma_site ~ dunif(0,100)
for(i in 1:nsites){
tau[i] <- 1/sigma[i]^2
sigma[i] ~ dunif(0,100)
}
# LIKELIHOOD
for(i in 1:n){
mass[i] ~ dnorm(mu[i],tau[site[i]])
mu[i] <- alpha[site[i]] #Mean will vary by site. 3 alphas, 1 per site. loop through every row of your dataset.
} # i
# DERIVED QUANTITIES
for(i in 1:nsites){
avg[i] <- (alpha[i]*stdev_data) + mean_data
stan_dev[i] <- (sigma[i]*stdev_data)
}
mu_site_m <- (mu_site*stdev_data) + mean_data
sigma_site_m <- (sigma_site*stdev_data)
} # end of model
",fill = TRUE)
sink()
win.data <- list(mass   = as.numeric(ceph_sites2$AVG_WT),
site   = ceph_sites2$id,
n      = nrow(ceph_sites2),
nsites = length(unique(ceph_sites2$id)),
stdev_data = sd(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE),
mean_data = mean(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE))
# Initial values
inits <- function()list(alpha = rnorm(7), sigma = rep(rlnorm(1),7))
# Parameters monitored
params <- c("alpha",
"sigma",
"avg",
"stan_dev",
"mu_site_m",
"sigma_site_m"
#"Bayes.P"
#"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
out_stwt <- jags(win.data, inits, params, "cephs_sitewt.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_stwt, dig = 1)
#Visualize Data-FIX THIS
boxplot(as.numeric(AVG_WT) ~ REGION,
ylab = "Average wt of Cepalopod",
xlab = "Site",
# outline = TRUE,
# notch = FALSE,
# staplewex = 0.2,
# boxwex = 0.4,
# border = TRUE,
data = ceph_sites2)
is.unique(ceph_sites2$REGION)
unique(ceph_sites2$REGION)
ceph_sites2
tail(ceph_sites2)
#Questions about this one: can you do a p val for an anova? Does the calc look the same?
#wANT TO REMAKE data next
#Add random effects? can you do that to an anova
#ANOVA
#Only looking at most fished sites
#Different variances?
#Visualize Data-FIX THIS
# boxplot(as.numeric(AVG_WT) ~ REGION,
#         ylab = "Average wt of Cepalopod",
#         xlab = "Site",
#         # outline = TRUE,
#         # notch = FALSE,
#         # staplewex = 0.2,
#         # boxwex = 0.4,
#         # border = TRUE,
#         data = ceph_sites2)
sink("cephs_sitewt.txt")
cat("
model {
# PRIORS
for(i in 1:nsites){
alpha[i] ~ dnorm(mu_site,tau_site)
}
mu_site ~ dnorm(0,0.001)
tau_site <- 1/sigma_site^2
sigma_site ~ dunif(0,100)
for(i in 1:nsites){
tau[i] <- 1/sigma[i]^2
sigma[i] ~ dunif(0,100)
}
# LIKELIHOOD
for(i in 1:n){
mass[i] ~ dnorm(mu[i],tau[site[i]])
mu[i] <- alpha[site[i]] #Mean will vary by site. 3 alphas, 1 per site. loop through every row of your dataset.
} # i
# DERIVED QUANTITIES
for(i in 1:nsites){
avg[i] <- (alpha[i]*stdev_data) + mean_data
stan_dev[i] <- (sigma[i]*stdev_data)
}
mu_site_m <- (mu_site*stdev_data) + mean_data
sigma_site_m <- (sigma_site*stdev_data)
# generate new data from the fitted model
for(i in 1:n){
y.new[i] ~ dnorm(mu[site[i]],tau[site[i]])   #Generates new data for each run of mcmc
}
} # end of model
",fill = TRUE)
sink()
win.data <- list(mass   = as.numeric(ceph_sites2$AVG_WT),
site   = ceph_sites2$id,
n      = nrow(ceph_sites2),
nsites = length(unique(ceph_sites2$id)),
stdev_data = sd(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE),
mean_data = mean(as.numeric(ceph_sites2$AVG_WT), na.rm = TRUE))
# Initial values
inits <- function()list(alpha = rnorm(7), sigma = rep(rlnorm(1),7))
# Parameters monitored
params <- c("alpha",
"sigma",
"avg",
"stan_dev",
"mu_site_m",
"sigma_site_m"
#"Bayes.P"
#"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
out_stwt <- jags(win.data, inits, params, "cephs_sitewt.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_stwt, dig = 1)
ceph_sites2
purps <- "#440154FF"
midnight <- "#414487FF"
dkteal <- "#2A788EFF"
limabean <- "#7AD151FF"
bananas <- "#FDE725FF"
par(mfrow = c(3,3),
mar = c(5.1, 4.1, 3.1, 0.5)) #b, l ,t ,r
boxplot(AVG_WT ~ REGION,
ylab = "Wt",
xlab = "Site",
staplewex = 0.2,
boxwex = 0.4,
frame.plot = FALSE,
col = c(purps,limabean,bananas),
data = ceph_sites2,
main = "Actual data")
boxplot(as.numeric(AVG_WT) ~ REGION,
ylab = "Wt",
xlab = "Site",
staplewex = 0.2,
boxwex = 0.4,
frame.plot = FALSE,
col = c(purps,limabean,bananas),
data = ceph_sites2,
main = "Actual data")
for(i in 1:8){
boxplot(out_stwt$BUGSoutput$sims.list$y.new[i,] ~ ceph_sites2$id,
ylab = "Wt",
xlab = "Site",
staplewex = 0.2,
boxwex = 0.4,
frame.plot = FALSE,
col = c(purps,limabean,bananas),
main = paste0("Model-generated data; iter = ",i))
}
out_stwt$BUGSoutput$sims.list$y.new
out_stwt$BUGSoutput
out_stwt$BUGSoutput$sims.list
out_stwt$BUGSoutput$sims.list$y.new
# Parameters monitored
params <- c("alpha",
"sigma",
"avg",
"stan_dev",
"mu_site_m",
"sigma_site_m",
#"Bayes.P"
"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
out_stwt <- jags(win.data, inits, params, "cephs_sitewt.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
out_stwt <- jags(win.data, inits, params, "cephs_sitewt.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_stwt, dig = 1)
par(mfrow = c(3,3),
mar = c(5.1, 4.1, 3.1, 0.5)) #b, l ,t ,r
boxplot(as.numeric(AVG_WT) ~ REGION,
ylab = "Wt",
xlab = "Site",
staplewex = 0.2,
boxwex = 0.4,
frame.plot = FALSE,
col = c(purps,limabean,bananas),
data = ceph_sites2,
main = "Actual data")
for(i in 1:8){
boxplot(out_stwt$BUGSoutput$sims.list$y.new[i,] ~ ceph_sites2$id,
ylab = "Wt",
xlab = "Site",
staplewex = 0.2,
boxwex = 0.4,
frame.plot = FALSE,
col = c(purps,limabean,bananas),
main = paste0("Model-generated data; iter = ",i))
}
boxplot(as.numeric(AVG_WT) ~ REGION,
ylab = "Wt",
xlab = "Site",
staplewex = 0.2,
boxwex = 0.4,
frame.plot = FALSE,
col = c(purps,limabean,bananas),
data = ceph_sites2,
main = "Actual data")
