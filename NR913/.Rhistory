# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = revi_pfor,
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(1, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
for(i in 1:nsite){
lambda[i] ~ dunif(0,10) # dgamma(.5,.5)
for(j in 1:nsurvey){
p[i,j] ~ dunif(0,1)
}}
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
logit(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = revi_pfor,
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(1, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
revi_pfor
nrow(revi_count)
revi_pfor
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
for(i in 1:nsite){
lambda[i] ~ dunif(0,10) # dgamma(.5,.5)
for(j in 1:nsurvey){
p[i,j] ~ dunif(0,1)
}}
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
logit(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = revi_pfor,
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(1, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
for(i in 1:nsite){
lambda[i] ~ dunif(0,10) # dgamma(.5,.5)
for(j in 1:nsurvey){
p[i,j] ~ dunif(0,1)
}}
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = revi_pfor,
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(1, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
rep(1, nrow(revi)
)
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = revi_pfor,
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(5, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
getwd()
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = revi_pfor,
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(5, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
class(revi_pfor)
closeAllConnections()
closeAllConnections()
class(revi_pfor)
# data
win.data <- list(y = revi_count,
forest = as.numeric(revi_pfor),
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = as.numeric(revi_pfor),
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(5, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
revi_obs[1,] <- c(1,2,3)
revi_obs
?colnames
colnames(revi_obs) <- c(1,2,3)
# isolate observation covariate matrix
revi_obs <- as.matrix(revi[,5:7], nrow = nrow(revi), ncol = 3)
colnames(revi_obs) <- c(1,2,3)
revi_obs
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = as.numeric(revi_pfor),
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(5, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
revi_obs <- as.integer(as.factor(sunstr(revi_survey_1_obs, 4, 4)))
revi_obs <- as.integer(as.factor(substr(revi_survey_1_obs, 4, 4)))
revi_obs <- as.integer(as.factor(substr(revi_obs$survey_1_obs, 4, 4)))
revi_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
revi_obs$surv_1_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
# isolate observation covariate matrix
revi_obs <- revi[,5:7]
revi_obs$surv_1_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
revi_obs$surv_1_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
revi_obs$surv_2_obs <- as.integer(as.factor(substr(revi_obs$surv_2_obs, 4, 4)))
revi_obs$surv_3_obs <- as.integer(as.factor(substr(revi_obs$surv_3_obs, 4, 4)))
# isolate observation covariate matrix
revi_obs <- revi[,5:7]
revi_obs$surv_1_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
revi_obs$surv_2_obs <- as.integer(as.factor(substr(revi_obs$surv_2_obs, 4, 4)))
revi_obs$surv_3_obs <- as.integer(as.factor(substr(revi_obs$surv_3_obs, 4, 4)))
revi
revi_obs
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = as.numeric(revi_pfor),
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(5, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_null, dig = 2)
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_null, dig = 2)
