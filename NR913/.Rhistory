##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = revi_pfor,
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(5, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
class(revi_pfor)
closeAllConnections()
closeAllConnections()
class(revi_pfor)
# data
win.data <- list(y = revi_count,
forest = as.numeric(revi_pfor),
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = as.numeric(revi_pfor),
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(5, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
revi_obs[1,] <- c(1,2,3)
revi_obs
?colnames
colnames(revi_obs) <- c(1,2,3)
# isolate observation covariate matrix
revi_obs <- as.matrix(revi[,5:7], nrow = nrow(revi), ncol = 3)
colnames(revi_obs) <- c(1,2,3)
revi_obs
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = as.numeric(revi_pfor),
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(5, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
revi_obs <- as.integer(as.factor(sunstr(revi_survey_1_obs, 4, 4)))
revi_obs <- as.integer(as.factor(substr(revi_survey_1_obs, 4, 4)))
revi_obs <- as.integer(as.factor(substr(revi_obs$survey_1_obs, 4, 4)))
revi_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
revi_obs$surv_1_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
# isolate observation covariate matrix
revi_obs <- revi[,5:7]
revi_obs$surv_1_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
revi_obs$surv_1_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
revi_obs$surv_2_obs <- as.integer(as.factor(substr(revi_obs$surv_2_obs, 4, 4)))
revi_obs$surv_3_obs <- as.integer(as.factor(substr(revi_obs$surv_3_obs, 4, 4)))
# isolate observation covariate matrix
revi_obs <- revi[,5:7]
revi_obs$surv_1_obs <- as.integer(as.factor(substr(revi_obs$surv_1_obs, 4, 4)))
revi_obs$surv_2_obs <- as.integer(as.factor(substr(revi_obs$surv_2_obs, 4, 4)))
revi_obs$surv_3_obs <- as.integer(as.factor(substr(revi_obs$surv_3_obs, 4, 4)))
revi
revi_obs
# JAGS model
sink("nmix.txt") #N NEEDS INITS FOR EACH N.
cat("
model {
##############
### PRIORS ###
##############
beta0 ~ dnorm(0,0.001)  # intercept of forest
beta1 ~ dnorm(0,0.001)  # effect of forest
beta2 ~ dnorm(0,0.001)  # intercept of observor
beta3 ~ dnorm(0,0.001)  # effect of observer
##################
### LIKELIHOOD ###
##################
# Abundance Model
for(i in 1:nsite){
n[i] ~ dpois(lambda[i]) #n is latent/non obs. therefore needs init val but not a prior?
log(lambda[i]) <- beta0 + beta1 * forest[i] #log of lambda will be all covariates
# Detection Model
for(j in 1:nsurvey){
y[i,j] ~ dbin(p[i,j], n[i]) #y is data, p is detection prob. p needs a prior? p i,j
logit(p[i,j]) <-  beta2 + beta3 * obs[i,j] #p needs a logit link
} #j
} #i
##########################
### DERIVED PARAMETERS ###
##########################
abun <- sum(n[])
} # end of model
",fill = TRUE)
sink()
# data
win.data <- list(y = revi_count,
forest = as.numeric(revi_pfor),
obs = revi_obs,
nsurvey = ncol(revi_count),
nsite = nrow(revi_count)
)
# Initial values
inits <- function()list(n = rep(5, nrow(revi)))#(Nst = rep(rep(revi_count, nrow(revi))))
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "n", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_null, dig = 2)
# Parameters monitored
params <- c("beta0", "beta1", "beta2", "beta3", "p", "abun")
# MCMC settings
nc <- 3; nt <- 1; ni <- 3000; nb <- 500
# run the model
out_null <- jags(win.data, inits, params, "nmix.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_null, dig = 2)
knitr::opts_chunk$set(echo = TRUE)
library(R2jags)
library(tidyr)
library(dplyr)
setwd("~/UNH_Docs/NR913")
cephs <- read.csv("C:/Users/sophi/Documents/UNH_Docs/Proposal/CNH2_data/cleanData.csv")
site_data <- cephs %>%
group_by(REGION) %>%
summarise(Count = n()) %>%
filter(Count > 150)
ceph_sites <- cephs %>%
filter(REGION %in% site_data$REGION)
ceph_sites2 <- transform(ceph_sites,id=as.numeric(factor(REGION)))
ceph_model <- transform(cephs,id=as.numeric(factor(REGION)), sex_id=(as.numeric(factor(FISHER_SEX))-1))
ceph_sex <- ceph_model %>% drop_na(sex_id)
unique(ceph_sites2$REGION)
length(unique(cephs$REGION))
unique(cephs$FISHER_NAME)
length(unique(cephs$FISHER_NAME))
#Questions about this one: can you do a p val for an anova? Does the calc look the same?
#ANOVA
#Only looking at most fished sites
#Visualize Data-FIX THIS
boxplot(AVG_WT ~ REGION,
ylab = "Average wt of Cepalopod",
xlab = "Site",
outline = TRUE,
notch = FALSE,
staplewex = 0.2,
boxwex = 0.4,
border = TRUE,
data = ceph_sites2)
knitr::opts_chunk$set(echo = TRUE)
library(R2jags)
library(tidyr)
library(dplyr)
setwd("~/UNH_Docs/NR913")
cephs <- read.csv("C:/Users/sophi/Documents/UNH_Docs/Proposal/CNH2_data/cleanData.csv")
site_data <- cephs %>%
group_by(REGION) %>%
summarise(Count = n()) %>%
filter(Count > 150)
ceph_sites <- cephs %>%
filter(REGION %in% site_data$REGION)
ceph_sites2 <- transform(ceph_sites,id=as.numeric(factor(REGION)))
ceph_model <- transform(cephs,id=as.numeric(factor(REGION)), sex_id=(as.numeric(factor(FISHER_SEX))-1))
ceph_sex <- ceph_model %>% drop_na(sex_id)
#Visualize Data-FIX THIS
boxplot(AVG_WT ~ REGION,
ylab = "Average wt of Cepalopod",
xlab = "Site",
outline = TRUE,
notch = FALSE,
staplewex = 0.2,
boxwex = 0.4,
border = TRUE,
data = ceph_sites2)
ceph_sites2
#ANOVA
#Visualize Data-FIX THIS
#CHANGE TO POISSON?
boxplot(NO_CAUGHT ~ REGION,
ylab = "No per catch",
xlab = "Site",
outline = TRUE,
notch = FALSE,
staplewex = 0.2,
boxwex = 0.4,
border = TRUE,
data = ceph_sites2)
#Visualize Data-FIX THIS
boxplot(AVG_WT ~ REGION,
# ylab = "Average wt of Cepalopod",
# xlab = "Site",
# outline = TRUE,
# notch = FALSE,
# staplewex = 0.2,
# boxwex = 0.4,
# border = TRUE,
data = ceph_sites2)
class(ceph_sites2$AVG_WT)
#Visualize Data-FIX THIS
boxplot(as.numeric(AVG_WT) ~ REGION,
# ylab = "Average wt of Cepalopod",
# xlab = "Site",
# outline = TRUE,
# notch = FALSE,
# staplewex = 0.2,
# boxwex = 0.4,
# border = TRUE,
data = ceph_sites2)
#Visualize Data-FIX THIS
boxplot(as.numeric(AVG_WT) ~ REGION,
ylab = "Average wt of Cepalopod",
xlab = "Site",
outline = TRUE,
notch = FALSE,
staplewex = 0.2,
boxwex = 0.4,
border = TRUE,
data = ceph_sites2)
knitr::opts_chunk$set(echo = TRUE)
library(R2jags)
library(tidyr)
library(dplyr)
setwd("~/UNH_Docs/NR913")
cephs <- read.csv("C:/Users/sophi/Documents/UNH_Docs/Proposal/CNH2_data/cleanData.csv")
site_data <- cephs %>%
group_by(REGION) %>%
summarise(Count = n()) %>%
filter(Count > 150)
ceph_sites <- cephs %>%
filter(REGION %in% site_data$REGION)
ceph_sites2 <- transform(ceph_sites,id=as.numeric(factor(REGION)))
ceph_model <- transform(cephs,id=as.numeric(factor(REGION)), sex_id=(as.numeric(factor(FISHER_SEX))-1))
ceph_sex <- ceph_model %>% drop_na(sex_id)
#Questions about this one: can you do a p val for an anova? Does the calc look the same?
#ANOVA
#Only looking at most fished sites
#Visualize Data-FIX THIS
boxplot(AVG_WT ~ REGION,
ylab = "Average wt of Cepalopod",
xlab = "Site",
outline = TRUE,
notch = FALSE,
staplewex = 0.2,
boxwex = 0.4,
border = TRUE,
data = ceph_sites2)
#Visualize Data-FIX THIS
boxplot(AVG_WT ~ REGION,
ylab = "Average wt of Cepalopod",
xlab = "Site",
# outline = TRUE,
# notch = FALSE,
# staplewex = 0.2,
# boxwex = 0.4,
# border = TRUE,
data = ceph_sites2)
class(ceph_sites2$AVG_WT)
class(ceph_sites2$REGION)
#Visualize Data-FIX THIS
boxplot(as.numeric(AVG_WT) ~ REGION,
ylab = "Average wt of Cepalopod",
xlab = "Site",
# outline = TRUE,
# notch = FALSE,
# staplewex = 0.2,
# boxwex = 0.4,
# border = TRUE,
data = ceph_sites2)
#ANOVA
#Visualize Data-FIX THIS
#CHANGE TO POISSON?
boxplot(NO_CAUGHT ~ REGION,
ylab = "No per catch",
xlab = "Site",
outline = TRUE,
notch = FALSE,
staplewex = 0.2,
boxwex = 0.4,
border = TRUE,
data = ceph_sites2)
head(ceph_sites2)
sink("cephs_siteno.txt")
cat("
model {
# PRIORS
for(i in 1:nsites){       #loop for alphas. can also do alpha[1] alpha[2]
alpha[i] ~ dnorm(0,0.001)
}
tau <- 1/sigma^2
sigma ~ dunif(0,100) #This gives only 1 variance across all sites
# LIKELIHOOD
for(i in 1:n){
mass[i] ~ dnorm(mu[i],tau)
mu[i] <- alpha[site[i]] #Mean will vary by site. 3 alphas, 1 per site. loop through every row of your dataset.
} # i
# # derived quantitites
# diff23 <- alpha[3] - alpha[2]
# # generate new data from the fitted model
# for(i in 1:n){
# y.new[i] ~ dnorm(mu[i],tau)   #Generates new data for each run of mcmc
#  }
} # end of model
",fill = TRUE)
sink()
win.data <- list(mass   = ceph_sites2$NO_CAUGHT,
site   = ceph_sites2$id,
n      = nrow(ceph_sites2),
nsites = length(unique(ceph_sites2$id)))
# Initial values
inits <- function()list(alpha = rnorm(7), sigma = rlnorm(1))
# Parameters monitored
params <- c("alpha",
"sigma"
#"Bayes.P"
#"y.new"
)
# MCMC settings
ni <- 1000; nt <- 3; nb <- 100; nc <- 3
out_stno <- jags(win.data, inits, params, "cephs_siteno.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_stno, dig = 1)
#Visualize Data-FIX THIS
boxplot(NO_CAUGHT ~ FISHER_SEX,
ylab = "No caught per landing",
xlab = "Site",
outline = TRUE,
notch = FALSE,
staplewex = 0.2,
boxwex = 0.4,
border = TRUE,
data = ceph_model)
sink("cephs_sexno.txt")
cat("
model {
# PRIORS
beta0 ~ dnorm(0,0.001)  # intercept
beta1 ~ dnorm(0,0.001)  # effect of M/F, IDK
# LIKELIHOOD
for(i in 1:n){
cephs[i] ~ dpois(lambda[i]) #error term built in bc lambda is both mean and var
log(lambda[i]) <- beta0 + beta1 * sex[i] # sex needs to be 0s and 1s. predicting log of lambda, not lambda
#lambda[i] <- exp(beta0 + beta1 * sex[i] ) Alternative form
} # i
} # end of model
",fill = TRUE)
sink()
win.data <- list(cephs = ceph_sex$NO_CAUGHT,
sex  = ceph_sex$sex_id,
n  = nrow(ceph_sex))
# Initial values
inits <- function()list(beta0  = rnorm(1),
beta1  = rnorm(1))
# Parameters monitored
params <- c("beta0",
"beta1")#,
# MCMC settings
ni <- 3000; nt <- 1; nb <- 1000; nc <- 3
out_sexno <- jags(win.data, inits, params, "cephs_sexno.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
ceph_sites
ceph_sex
#Visualize Data-FIX THIS
boxplot(NO_CAUGHT ~ FISHER_SEX,
ylab = "No caught per landing",
xlab = "Site",
outline = TRUE,
notch = FALSE,
staplewex = 0.2,
boxwex = 0.4,
border = TRUE,
data = ceph_model)
sink("cephs_sexno.txt")
cat("
model {
# PRIORS
beta0 ~ dnorm(0,0.001)  # intercept
beta1 ~ dnorm(0,0.001)  # effect of M/F, IDK
# LIKELIHOOD
for(i in 1:n){
cephs[i] ~ dpois(lambda[i]) #error term built in bc lambda is both mean and var
log(lambda[i]) <- beta0 + beta1 * sex[i] # sex needs to be 0s and 1s. predicting log of lambda, not lambda
#lambda[i] <- exp(beta0 + beta1 * sex[i] ) Alternative form
} # i
} # end of model
",fill = TRUE)
sink()
win.data <- list(cephs = ceph_sex$NO_CAUGHT,
sex  = ceph_sex$sex_id,
n  = nrow(ceph_sex))
# Initial values
inits <- function()list(beta0  = rnorm(1),
beta1  = rnorm(1))
# Parameters monitored
params <- c("beta0",
"beta1")#,
# MCMC settings
ni <- 3000; nt <- 1; nb <- 1000; nc <- 3
out_sexno <- jags(win.data, inits, params, "cephs_sexno.txt",
n.chains = nc, n.thin = nt, n.iter = ni, n.burnin = nb,
working.directory = getwd())
print(out_sexno, dig = 2)
